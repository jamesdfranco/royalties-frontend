# Royalties Frontend

Next.js frontend for the Royalties marketplace.

## Setup

1. Clone the repo
2. Run `npm install`
3. Copy `env.example.txt` to `.env.local` and add your RPC URL
4. Run `npm run dev`

## Environment Variables

See `env.example.txt` for required variables.
